import React from "react";
import "../styles/common.css";

function NextBtnComplete({children}) {
    return (
        <div className="next-btn-complete">{children}</div>
    )
}

export default NextBtnComplete;